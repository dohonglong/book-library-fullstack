const initialState = {
  isAuthenticated: false,
  user: null,
}

const authReducer = (state = initialState, actions) => {
  switch (actions.type) {
    case value:
      break

    default:
      break
  }
}

export default authReducer
